name: North Sea pilot estimate

on:
  workflow_dispatch:

jobs:
  estimate:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Estimate processing raster
        run: |
          python scripts/estimate_north_sea_pilot.py

      - name: Install PMTiles CLI
        run: |
          go install github.com/protomaps/go-pmtiles@latest
          echo "$(go env GOPATH)/bin" >> "$GITHUB_PATH"

      - name: Estimate Mapterhorn extract
        run: |
          "$(go env GOPATH)/bin/pmtiles" extract \
            --bbox=1.5,50.5,9.5,55.5 \
            --maxzoom=11 \
            --overfetch=0 \
            --dry-run \
            https://download.mapterhorn.com/planet.pmtiles \
            north-sea-dem.pmtiles

      - name: Inspect OSM ocean source
        run: |
          curl -fsSIL https://osmdata.openstreetmap.de/download/water-polygons-split-3857.zip
